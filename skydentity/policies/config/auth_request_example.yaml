authorization:
  cloud_provider: 
    - gcp
  
  project:
    - sky-identity

  actions: 
    - CREATE

  roles:
    - restricted_role:
      - role: roles/storage.objectUser
      - scope: bucket
      - object: http://storage.googleapis.com/test_bucket_signedurl